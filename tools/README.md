# Build-time tools

`build.ps1` downloads `yt-dlp.exe`, a static `ffmpeg.exe`, and `deno.exe` into this directory.
They are embedded into the final `YouTube2MP3.exe` and do not need to be shipped separately.
